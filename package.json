{
  "dependencies": {
    "csv-parser": "^3.0.0"
  },
  "name": "homework-files-f",
  "description": "Using the CSV file provided, answer the following questions by reading the file using sequences, not the entire file at once.",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
